# Trino Connection Configuration
TRINO_HOST=localhost
TRINO_PORT=8080
TRINO_USER=trino
TRINO_HTTP_SCHEME=http

# Optional: Default catalog and schema
TRINO_CATALOG=
TRINO_SCHEMA=

# Authentication Options:

# Option 1: Basic Authentication (username/password)
TRINO_PASSWORD=

# Option 2: OAuth2 Authentication
# Note: The Trino Python client requires implementing a redirect handler
# for OAuth2. For production use, you'll need to implement the OAuth2 flow
# with your identity provider. This server is designed to work with Trino's
# OAuth2 authentication mechanism once properly configured.
# See: https://trino.io/docs/current/security/oauth2.html

