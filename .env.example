# Trino Connection Configuration
TRINO_HOST=localhost
TRINO_PORT=8080
TRINO_USER=trino
TRINO_HTTP_SCHEME=http

# Optional: Default catalog and schema
TRINO_CATALOG=
TRINO_SCHEMA=

# Query Execution Permissions:
# Set to "true" to enable the execute_query tool (can run write operations)
# Set to "false" (default) to restrict execute_query tool to return an error (blocks execution)
# Note: execute_query_read_only tool is always available for read-only queries
ALLOW_WRITE_QUERIES=false

# Authentication Options:

# Option 1: Basic Authentication (username/password)
TRINO_PASSWORD=

# Option 2: OAuth2 Authentication
AUTH_METHOD="OAUTH2" # Set to "OAUTH2" to enable OAuth2 authentication

# Option 3: Azure Service Principal (SPN) Authentication
# Tries AzureCliCredential (az login) first, then ClientSecretCredential, then DefaultAzureCredential.
# AZURE_SCOPE is always required. AZURE_CLIENT_ID/SECRET/TENANT_ID are needed if not using az login.
# AUTH_METHOD="AZURE_SPN"
# AZURE_SCOPE=api://your-trino-app-id/.default
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_TENANT_ID=your-tenant-id
