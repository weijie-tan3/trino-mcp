[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --cov=src/trino_mcp
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
