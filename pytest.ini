[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = 
    -v
    --strict-markers
    --cov=src/trino_mcp
    --cov-branch
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
